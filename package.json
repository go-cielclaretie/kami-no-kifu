{
  "name": "kifu-print-web",
  "version": "2.0.0",
  "private": true,
  "description": "Client-side SGF to print-ready PDF/PNG/JPG. Static GitHub Pages edition.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check-site.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
