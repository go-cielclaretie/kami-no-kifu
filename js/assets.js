/* Texture stays on the same origin, keeping Canvas exports untainted. */
(function (root) {
  'use strict';
  root.KifuAssets = Object.freeze({
    itame: root.KifuPlatform.assetURL('assets/itame-grain.png?v=2.0.0')
  });
})(globalThis);
